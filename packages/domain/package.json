{
  "name": "@kassist/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "tsc --noEmit --pretty false",
    "typecheck": "tsc --noEmit --pretty false",
    "test": "pnpm exec tsx --test src/foundation.test.ts",
    "test:unit": "pnpm exec tsx --test src/foundation.test.ts",
    "test:integration": "pnpm exec tsx --test src/foundation.test.ts",
    "test:e2e": "pnpm exec tsx --test src/foundation.test.ts",
    "build": "tsc --noEmit --pretty false",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "tsx": "4.23.12"
  }
}
