# AI Boundary

The domain depends on `LLMProvider`, not Ollama-specific APIs.

The provider may interpret input and produce structured output, but Core validation remains authoritative for business rules and critical commands.
