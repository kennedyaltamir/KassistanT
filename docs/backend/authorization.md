# Authorization

Status: PARTIAL / MISSING.

Provisioning Service is defined as authorized to authorize enrollment, revoke devices, rotate keys and read device status. Endpoint-by-endpoint authorization matrix is not fully defined. Future SaaS roles are not MVP requirements.