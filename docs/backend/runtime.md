# Backend Runtime

Status: CURRENT bootstrap / DEFINED architecture.

Startup contract: load configuration, open database, run health checks and expose ready/diagnostic state. Heavy inference must not block UI. Current Gateway and WSS runtime are skeleton/NOT_IMPLEMENTED.
