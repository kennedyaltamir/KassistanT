# Observability

Status: DEFINED / PARTIAL.

Logs require timestamp, level, category, event, correlation_id, entity, result and error code. Secrets and unnecessary conversation payloads are excluded. Baseline metrics include AI latency/errors, order outcomes, provider failures, queue depth, ACK timeout and backup failures.