# Integration Boundaries

WhatsApp and Google integrations are represented by adapter boundaries only in the bootstrap.

No production credentials, OAuth clients, webhook secrets or external provider connectivity are included in this increment.
