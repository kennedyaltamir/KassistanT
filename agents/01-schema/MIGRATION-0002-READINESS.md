# IA-01 — Migration 0002 Readiness

Status: **NON-AUTHORITATIVE HISTORICAL ARTIFACT / REPLACEMENT NOT AUTHORIZED**
Migration `0002`: **PRESENT PHYSICALLY, NOT NORMATIVE**

## Reconciliation

`GOV-DRIFT-0002` was resolved by Operator Option B. The existing `apps/desktop/database/migrations/0002_c1_product_order.sql` remains untouched and is classified as historical/non-authoritative.

Its presence does not authorize execution or establish it as the canonical schema baseline.

## Current table readiness

| Category | Tables |
|---|---|
| READY_AFTER_LOCAL_DECISION | `store`, `product_image`, `log` |
| READY_AFTER_CROSS_AGENT_DECISION | `device`, `product`, `product_modifier`, `promotion`, `customer`, `conversation`, `message`, `order`, `notification`, `inbound_inbox`, `job`, `audit_log`, `ai_profile`, `ai_execution` |
| READY_AFTER_RESOLVED_GLOBAL_SEMANTICS | `domain_outbox` — semantic ownership resolved; physical DDL still requires implementation-level determinism |
| READY_AFTER_EXTERNAL_DECISION | none |
| BLOCKED | `settings`, `product_category`, `customer_address`, `order_item`, `order_item_modifier`, `order_status_history`, `payment_method`, `integration`, `integration_credential`, `knowledge_item` |

These categories describe physical determinism, not authorization to implement runtime behavior.

## CONTRACT-001 effect

DomainOutbox is no longer globally blocked by ownership semantics. Current normative model:

- Domain defines event intent.
- IA-03 owns durable Outbox mechanics and worker.
- Business state and event intent share the required atomic transaction boundary where applicable.
- Provider invocation occurs after durable intent.

Physical fields, worker implementation, retry strategy and FK actions remain implementation work and are not generated here.

## Migration gate

The existing 0002 file is not executable as the canonical MVP schema merely because it exists. Any future physical migration requires a separate implementation authorization after the relevant table contracts are deterministic.

No migration was executed, deleted, renamed, replaced or generated by this reconciliation.
